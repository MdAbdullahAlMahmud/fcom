// Admin bKash/Nagad Account Settings Page (moved from payment-accounts)
'use client';
import PaymentAccountsPage from '../../payment-accounts/page';

export default function BkashNagadAccountSettingsPage() {
  // Just reuse the existing PaymentAccountsPage for now
  return <PaymentAccountsPage />;
}
